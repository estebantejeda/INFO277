import { FiltroKardexPipe } from './filtro-kardex.pipe';

describe('FiltroKardexPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroKardexPipe();
    expect(pipe).toBeTruthy();
  });
});
